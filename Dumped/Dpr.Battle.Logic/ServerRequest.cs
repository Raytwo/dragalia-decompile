namespace Dpr.Battle.Logic;

public enum ServerRequest : byte
{
	NONE,
	SETUP,
	SELECT_ACTION,
	SELECT_CHANGE_OR_ESCAPE,
	SELECT_COVER_POKE,
	SELECT_CHANGE_POKE,
	CONFIRM_COUNTER_POKECHANGE,
	SERVER_COMMAND_FIRST_BATTLE_IN,
	SERVER_COMMAND_POKECHANGE_AFTERFIRSTPOKEIN,
	SERVER_COMMAND_AFTER_ACTION_SELECT,
	SERVER_COMMAND_AFTER_COVER_POKE_SELECT,
	SERVER_COMMAND_AFTER_INTERRUPT_POKECHANGE,
	SERVER_COMMAND_ESCAPE_BY_CHANGE_OR_ESCAPE,
	RECORD_BATTLE_START_TIMING,
	RECORD_BATTLE_START_CHAPTER,
	RECORD_POKECHANGE_AFTERFIRSTPOKEIN,
	RECORD_ACTION,
	RECORD_COVER_POKE,
	RECORD_INTERRUPT_POKECHANGE,
	RECORD_TIMEUP_CHAPTER,
	NOTIFY_TIMEUP,
	EXIT_WILD_WIN,
	EXIT_WILD_LOSE,
	EXIT_WILD_CAPTURE,
	EXIT_WILD_FORCE,
	EXIT_COMM,
	EXIT_NPC,
	EXIT_BATTLE_SPOT_TRAINER,
	EXIT_FADEOUT,
	QUIT_BATTLE,
	SEND_LATEST_REPLY,
	SEND_CLIENT_LIMIT_TIME,
	SYNC_CLIENT_LIMIT_TIME,
	RAID_CAPTURE_START,
	RAID_CAPTURE_SELECT_BALL,
	RAID_CAPTURE_RESULT,
	RAID_SCMD_RESULT,
	RAID_EXIT_LOSE,
	NUM
}
